services:
  vector-db:
    image: qdrant/qdrant
    ports:
      - "6333:6333"

      #write in terminal docker compose up
      #for detaching the terminal press ctrl+c and then write docker compose up -d